{
  "name": "synora",
  "version": "0.1.0",
  "private": true,
  "description": "Distributed task scheduler and worker system",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "migrate": "node src/db/migrate.js",
    "queue:smoke": "node src/queue/smoke.js",
    "db:up": "docker compose up -d postgres redis",
    "db:down": "docker compose down"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "dotenv": "^16.6.1",
    "express": "^5.2.1",
    "pg": "^8.16.3",
    "redis": "^4.7.1"
  }
}
