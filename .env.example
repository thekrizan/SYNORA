# Application
PORT=3000

# PostgreSQL (matches docker-compose.yml defaults)
DATABASE_URL=postgresql://synora:synora@localhost:5432/synora

# Redis (reserved for Phase 2)
REDIS_URL=redis://localhost:6379

